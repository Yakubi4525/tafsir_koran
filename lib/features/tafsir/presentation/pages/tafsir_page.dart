import 'package:flutter/material.dart';

class TafsirPage extends StatelessWidget {
  const TafsirPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        elevation: 0.0,
      ),
      body: Center(
        child: Text('Tafsir Page'),
      ),
    );
  }
}
