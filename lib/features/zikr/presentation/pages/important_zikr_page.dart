import 'package:flutter/material.dart';

class ImportanatZikrPage extends StatelessWidget {
  const ImportanatZikrPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        elevation: 0.0,
      ),
      body: Center(
        child: Text('Экран  важные дуа'),
      ),
    );
  }
}
