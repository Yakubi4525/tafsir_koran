import 'package:flutter/material.dart';

class ZikrPage extends StatelessWidget {
  const ZikrPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        elevation: 0.0,
      ),
      body: Center(
        child: Text('Экран зикра'),
      ),
    );
  }
}
